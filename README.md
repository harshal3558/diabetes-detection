1. Import dataset
2. Classify categorical and numerical features.
3. Remove Duplicate Values
4. Find Missing Values (if categorical_feature have then use 'mode', if numerical_feature have then use 'mean','median').

Here, our data is ready to go for deep cleaning.

5. Do train-test-split.
6. Use pipeline and columntransformer to perform (missing values handling, onehot encoding, standardization, Model training).
7. Build Prediction and Evaluation model